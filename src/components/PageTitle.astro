---
interface Props {
	title: string;
	link_text: string;
	href: string;

}

const { href, title, link_text } = Astro.props;
---
<h1 class="text-md uppsercase text-center">{title}</h1>
<a class="font-mono text-sm underline mt-4" href={href}>View {link_text}</a>